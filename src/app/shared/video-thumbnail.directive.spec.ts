/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VideoThumbnailDirective } from './video-thumbnail.directive';

describe('VideoThumbnailDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoThumbnailDirective();
    expect(directive).toBeTruthy();
  });
});
