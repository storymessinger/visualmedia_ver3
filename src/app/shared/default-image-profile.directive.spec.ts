/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DefaultImageProfileDirective } from './default-image-profile.directive';

describe('DefaultImageProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultImageProfileDirective();
    expect(directive).toBeTruthy();
  });
});
